# Copy to .env. Only VITE_-prefixed vars reach the browser, and .env is read at
# build time, so dist/ is baked per environment.

# Router and Query devtools. Defaults on in dev, off in production builds.
# Setting this in a production build does nothing: the branch is compiled out.
VITE_ENABLE_DEVTOOLS=true
